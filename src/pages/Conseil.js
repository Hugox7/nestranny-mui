import React from 'react';

class Conseil extends React.Component {

    componentDidMount() {
        window.scrollTo(0, 0);
    }

    render() {
        return (
            <div>
                conseil
            </div>
        );
    }
}

export default Conseil;